import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import MdEditorView from './MdEditorView.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MdEditorView />
  </React.StrictMode>,
)
